<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Weighbridge"
             x:Class="Weighbridge.MainPage"
             x:DataType="local:MainPage"
             Title="Weighbridge System"
             BackgroundColor="#0E0E0E">

    <Grid Padding="24" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" ColumnDefinitions="1.2*,*" ColumnSpacing="24">

            <!-- LEFT PANEL -->
            <Border Grid.Column="0" BackgroundColor="#1A1A1A" Stroke="Transparent" StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="16" />
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="Black" Offset="0,4" Radius="16" Opacity="0.3" />
                </Border.Shadow>
                <ScrollView>
                    <Grid Padding="32" RowSpacing="24">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Modes -->
                        <Grid Grid.Row="0" ColumnDefinitions="*,*,*,*" ColumnSpacing="12">
                            <Border x:Name="TwoWeightsButton" Grid.Column="0" BackgroundColor="#00FF7F" StrokeThickness="0" Stroke="#444">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnTwoWeightsClicked" />
                                </Border.GestureRecognizers>
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="8" />
                                </Border.StrokeShape>
                                <Label Text="2 Weights" TextColor="#0E0E0E" HorizontalOptions="Center" Padding="8" FontSize="12" FontAttributes="Bold"/>
                            </Border>
                            <Border x:Name="EntryAndTareButton" Grid.Column="1" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnEntryAndTareClicked" />
                                </Border.GestureRecognizers>
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="8" />
                                </Border.StrokeShape>
                                <Label Text="Entry + Tare" TextColor="#CCCCCC" HorizontalOptions="Center" Padding="8" FontSize="12"/>
                            </Border>
                            <Border x:Name="TareAndExitButton" Grid.Column="2" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnTareAndExitClicked" />
                                </Border.GestureRecognizers>
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="8" />
                                </Border.StrokeShape>
                                <Label Text="Tare + Exit" TextColor="#CCCCCC" HorizontalOptions="Center" Padding="8" FontSize="12"/>
                            </Border>
                            <Border x:Name="SingleWeightButton" Grid.Column="3" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnSingleWeightClicked" />
                                </Border.GestureRecognizers>
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="8" />
                                </Border.StrokeShape>
                                <Label Text="Single Weight" TextColor="#CCCCCC" HorizontalOptions="Center" Padding="8" FontSize="12"/>
                            </Border>
                        </Grid>

                        <!-- Material Type Selector -->
                        <Border Grid.Row="1" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="12" />
                            </Border.StrokeShape>
                            <Grid ColumnDefinitions="Auto, *, Auto" Padding="16,12">
                                <Label Grid.Column="0" Text="‹" TextColor="#00FF7F" FontSize="24" VerticalOptions="Center" FontAttributes="Bold"/>
                                <Label Grid.Column="1" Text="Construction Materials" TextColor="White" FontSize="18" HorizontalOptions="Center" VerticalOptions="Center" FontAttributes="Bold"/>
                                <Label Grid.Column="2" Text="›" TextColor="#00FF7F" FontSize="24" VerticalOptions="Center" FontAttributes="Bold"/>
                            </Grid>
                        </Border>

                        <!-- Live Weight -->
                        <Border Grid.Row="2" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444" Margin="0,10,0,0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="12" />
                            </Border.StrokeShape>
                            <VerticalStackLayout Padding="20,24" Spacing="8">
                                <Label Text="LIVE WEIGHT" TextColor="#CCCCCC" HorizontalOptions="Center" FontSize="24" FontAttributes="Bold"/>
                                <Label Text="{Binding LiveWeight}" TextColor="White" HorizontalOptions="Center" FontSize="48" FontAttributes="Bold" />
                                <Label Text="KG" TextColor="#CCCCCC" HorizontalOptions="Center" FontSize="14" />
                            </VerticalStackLayout>
                        </Border>

                        <!-- Entrance / Exit / Net -->
                        <Grid Grid.Row="3" ColumnDefinitions="*,*,*" ColumnSpacing="16" VerticalOptions="FillAndExpand">
                            <!-- Entrance -->
                            <Border Grid.Column="0" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="12" />
                                </Border.StrokeShape>
                                <StackLayout Padding="20,24" Spacing="8">
                                    <Label Text="ENTRANCE" TextColor="#CCCCCC" HorizontalOptions="Center" FontSize="12" FontAttributes="Bold"/>
                                    <Label x:Name="WeightLabel" Text="{Binding EntranceWeight}" TextColor="White" HorizontalOptions="Center" FontSize="32" FontAttributes="Bold" />
                                    <Label Text="KG" TextColor="#CCCCCC" HorizontalOptions="Center" FontSize="14" />
                                    <Ellipse Fill="#00FF7F" WidthRequest="8" HeightRequest="8" HorizontalOptions="Center" />
                                </StackLayout>
                            </Border>

                            <!-- Exit -->
                            <Border Grid.Column="1" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="12" />
                                </Border.StrokeShape>
                                <StackLayout Padding="20,24" Spacing="8">
                                    <Label Text="EXIT" TextColor="#CCCCCC" HorizontalOptions="Center" FontSize="12" FontAttributes="Bold"/>
                                    <Label Text="{Binding ExitWeight}" TextColor="White" HorizontalOptions="Center" FontSize="32" FontAttributes="Bold" />
                                    <Label Text="KG" TextColor="#CCCCCC" HorizontalOptions="Center" FontSize="14" />
                                    <Ellipse Fill="#444" WidthRequest="8" HeightRequest="8" HorizontalOptions="Center" />
                                </StackLayout>
                            </Border>

                            <!-- Net Weight -->
                            <Border Grid.Column="2" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="12" />
                                </Border.StrokeShape>
                                <StackLayout Padding="20,24" Spacing="8">
                                    <Label Text="NET WEIGHT" TextColor="#CCCCCC" HorizontalOptions="Center" FontSize="12" FontAttributes="Bold"/>
                                    <Label Text="{Binding NetWeight}" TextColor="White" HorizontalOptions="Center" FontSize="32" FontAttributes="Bold" />
                                    <Label Text="KG" TextColor="#CCCCCC" HorizontalOptions="Center" FontSize="14" />
                                    <Ellipse Fill="#444" WidthRequest="8" HeightRequest="8" HorizontalOptions="Center" />
                                </StackLayout>
                            </Border>
                        </Grid>

                        <!-- Status -->
                        <Border Grid.Row="4" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="8" />
                            </Border.StrokeShape>
                            <Grid ColumnDefinitions="*,Auto,Auto" Padding="16,12">
                                <StackLayout Grid.Column="0" Orientation="Horizontal" Spacing="8">
                                    <Ellipse Fill="{Binding StabilityColor}" WidthRequest="8" HeightRequest="8" VerticalOptions="Center" />
                                    <Label Text="CONNECTED" TextColor="#00FF7F" FontSize="12" VerticalOptions="Center" FontAttributes="Bold"/>
                                    <Label x:Name="ConnectionStatusLabel" Grid.Column="2" Text="COM1 • 9600 bps" TextColor="#CCCCCC" FontSize="12" VerticalOptions="Center"/>
                                </StackLayout>
                            </Grid>
                        </Border>
                    </Grid>
                </ScrollView>
            </Border>

            <!-- RIGHT PANEL -->
            <Border Grid.Column="1" BackgroundColor="#1A1A1A" Stroke="Transparent" StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="16" />
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="Black" Offset="0,4" Radius="16" Opacity="0.3" />
                </Border.Shadow>

                <Grid Padding="20" RowSpacing="10" ColumnSpacing="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Transaction Header -->
                    <StackLayout Grid.Row="0" Spacing="4">
                        <Label Text="Transaction Details" TextColor="White" FontSize="18" FontAttributes="Bold" />
                        <Label Text="Complete the weighing transaction information" TextColor="#CCCCCC" FontSize="12" />
                    </StackLayout>

                    <!-- Vehicle -->
                    <Label Grid.Row="1" Text="VEHICLE" TextColor="#CCCCCC" FontSize="10" FontAttributes="Bold" />
                    <Border Grid.Row="2" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="8" />
                        </Border.StrokeShape>
                        <StackLayout>
                            <Picker x:Name="VehiclePicker" Title="Select Vehicle License" TextColor="White" TitleColor="#888" BackgroundColor="Transparent"
                                    ItemsSource="{Binding Vehicles}" SelectedItem="{Binding SelectedVehicle}" ItemDisplayBinding="{Binding LicenseNumber}" Margin="10,5" />
                            <Label x:Name="InProgressWarningLabel" TextColor="Orange" HorizontalOptions="Center" IsVisible="{Binding IsInProgressWarningVisible}" Text="{Binding InProgressWarningText}" Margin="0,0,0,5"/>
                        </StackLayout>
                    </Border>

                    <!-- Tare Weight -->
                    <Label Grid.Row="3" x:Name="TareWeightLabel" Text="TARE WEIGHT (KG)" TextColor="#CCCCCC" FontSize="10" FontAttributes="Bold" IsVisible="{Binding IsTareEntryVisible}" />
                    <Border Grid.Row="4" x:Name="TareWeightBorder" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444" IsVisible="{Binding IsTareEntryVisible}">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="8" />
                        </Border.StrokeShape>
                        <Entry x:Name="TareWeightEntry" Text="{Binding TareWeight}" TextColor="White" BackgroundColor="Transparent"
                                Placeholder="Enter Tare Weight" PlaceholderColor="#888" Margin="10,5" Keyboard="Numeric"/>
                    </Border>

                    <!-- Locations -->
                    <Label Grid.Row="5" Text="LOCATIONS" TextColor="#CCCCCC" FontSize="10" FontAttributes="Bold" />
                    <Grid Grid.Row="6" ColumnDefinitions="*,*" ColumnSpacing="10">
                        <Border Grid.Column="0" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="8" />
                            </Border.StrokeShape>
                            <Picker x:Name="SourceSitePicker" Title="Source Site" TextColor="White" TitleColor="#888" BackgroundColor="Transparent"
                                    ItemsSource="{Binding Sites}" SelectedItem="{Binding SelectedSourceSite}" ItemDisplayBinding="{Binding Name}" Margin="10,5" />
                        </Border>
                        <Border Grid.Column="1" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="8" />
                            </Border.StrokeShape>
                            <Picker x:Name="DestinationSitePicker" Title="Destination Site" TextColor="White" TitleColor="#888" BackgroundColor="Transparent"
                                    ItemsSource="{Binding Sites}" SelectedItem="{Binding SelectedDestinationSite}" ItemDisplayBinding="{Binding Name}" Margin="10,5" />
                        </Border>
                    </Grid>

                    <!-- Material & Parties -->
                    <Label Grid.Row="7" Text="MATERIAL &amp; PARTIES" TextColor="#CCCCCC" FontSize="10" FontAttributes="Bold" />
                    <Grid Grid.Row="8" ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="10" RowSpacing="10">
                        <Border Grid.Row="0" Grid.Column="0" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="8" />
                            </Border.StrokeShape>
                            <Picker x:Name="ItemPicker" Title="Select Material" TextColor="White" TitleColor="#888" BackgroundColor="Transparent"
                                    ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" ItemDisplayBinding="{Binding Name}" Margin="10,5" />
                        </Border>

                        <Border Grid.Row="0" Grid.Column="1" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="8" />
                            </Border.StrokeShape>
                            <Picker x:Name="CustomerPicker" Title="Select Customer" TextColor="White" TitleColor="#888" BackgroundColor="Transparent"
                                    ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" ItemDisplayBinding="{Binding Name}" Margin="10,5" />
                        </Border>

                        <Border Grid.Row="1" Grid.Column="0" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="8" />
                            </Border.StrokeShape>
                            <Picker x:Name="TransportPicker" Title="Select Transport Company" TextColor="White" TitleColor="#888" BackgroundColor="Transparent"
                                    ItemsSource="{Binding Transports}" SelectedItem="{Binding SelectedTransport}" ItemDisplayBinding="{Binding Name}" Margin="10,5" />
                        </Border>

                        <Border Grid.Row="1" Grid.Column="1" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="8" />
                            </Border.StrokeShape>
                            <Picker x:Name="DriverPicker" Title="Select Driver" TextColor="White" TitleColor="#888" BackgroundColor="Transparent"
                                    ItemsSource="{Binding Drivers}" SelectedItem="{Binding SelectedDriver}" ItemDisplayBinding="{Binding Name}" Margin="10,5" />
                        </Border>
                    </Grid>

                    <!-- Remarks -->
                    <Label Grid.Row="9" Text="REMARKS" TextColor="#CCCCCC" FontSize="10" FontAttributes="Bold" />
                    <Border Grid.Row="10" BackgroundColor="#2A2A2A" StrokeThickness="1" Stroke="#444">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="8" />
                        </Border.StrokeShape>
                        <Editor x:Name="RemarksEditor" Text="{Binding Remarks}" AutoSize="TextChanges" TextColor="White" BackgroundColor="Transparent"
                                Placeholder="Enter any additional notes..." PlaceholderColor="#888" Margin="10,5" />
                    </Border>

                    <!-- Buttons -->
                    <Grid Grid.Row="12" RowDefinitions="Auto,Auto" RowSpacing="10">
                        <Grid Grid.Row="0" ColumnDefinitions="*,*" ColumnSpacing="10">
                            <Button x:Name="FirstWeightButton" Grid.Column="0" Text="{Binding FirstWeightButtonText}" BackgroundColor="#FFD700" TextColor="#0E0E0E" CornerRadius="8" HeightRequest="40"
                                    FontAttributes="Bold" Clicked="OnToYardClicked" IsEnabled="{Binding IsWeightStable}" />
                            <Button x:Name="SecondWeightButton" Grid.Column="1" Text="SAVE and PRINT" BackgroundColor="#00FF7F" TextColor="#0E0E0E" CornerRadius="8" HeightRequest="40"
                                    FontAttributes="Bold" Clicked="OnSaveAndPrintClicked" IsVisible="{Binding IsSecondWeightButtonVisible}" IsEnabled="{Binding IsWeightStable}"/>
                        </Grid>

                        <Grid Grid.Row="1" ColumnDefinitions="*,*,*,*" ColumnSpacing="10">
                            <Button Grid.Column="0" Text="UPDATE TARE" BackgroundColor="#2A2A2A" TextColor="White" CornerRadius="8" HeightRequest="35"
                                    FontSize="10" Clicked="OnUpdateTareClicked" />
                            <Button Grid.Column="1" Text="CERTIFICATES" BackgroundColor="#2A2A2A" TextColor="White" CornerRadius="8" HeightRequest="35"
                                    FontSize="10" Clicked="OnCertificatesClicked" />
                            <Button Grid.Column="2" Text="REPORTS" BackgroundColor="#2A2A2A" TextColor="White" CornerRadius="8" HeightRequest="35"
                                    FontSize="10" Clicked="OnReportsClicked" />
                            <Button Grid.Column="3" x:Name="CancelDocketButton" Text="CANCEL DOCKET" BackgroundColor="#FF4500" TextColor="White" CornerRadius="8" HeightRequest="35"
                                    FontSize="10" Command="{Binding CancelDocketCommand}" IsEnabled="{Binding IsDocketLoaded}" />
                        </Grid>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</ContentPage>
